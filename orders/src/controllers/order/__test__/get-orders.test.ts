import request from "supertest";
import { app } from "../../../app";

it("return 200 on successfull fetching", async () => {
  // await request(app)
  //   .post("/api/products/categories")
  //   .set("Cookie", await global.signin("admin"))
  //   .send({ title: "category1" })
  //   .expect(201);
  // await request(app)
  //   .post("/api/products/categories")
  //   .set("Cookie", await global.signin("admin"))
  //   .send({ title: "category2" })
  //   .expect(201);
  // const res = await request(app).get("/api/products/categories").expect(200);
  // expect(res.body.length).toEqual(2);
});
